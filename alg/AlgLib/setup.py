from setuptools import setup, find_packages

setup(
    name='algUtil',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'matplotlib',
        'numpy'
    ],
    author='33may',
    author_email='eternalmay33@example.com',
    keywords=['sorting', 'visualization', 'algorithm']
)

#%%
